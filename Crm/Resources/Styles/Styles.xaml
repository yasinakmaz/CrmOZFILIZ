<?xml version="1.0" encoding="UTF-8" ?>
<?xaml-comp compile="true" ?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:md="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material">


    <Style TargetType="Page" x:Key="StlPage">
        <Setter Property="BackgroundColor" Value="{StaticResource PrimaryDark}"/>
    </Style>

    <Style TargetType="md:TextField" x:Key="Txt">
        <Setter Property="AccentColor" Value="{StaticResource White}"/>
        <Setter Property="TitleColor" Value="{StaticResource White}"/>
        <Setter Property="AllowClear" Value="True"/>
        <Setter Property="BorderColor" Value="{StaticResource White}"/>
        <Setter Property="Icon" Value="click.png"/>
        <Setter Property="DisallowClearButtonFocus" Value="False"/>
        <Setter Property="ReturnType" Value="Next"/>
        <Setter Property="FontFamily" Value="Roboto"/>
        <Setter Property="Margin" Value="0,0,10,0"/>
        <Setter Property="HeightRequest" Value="62"/>
        <Setter Property="IsTextPredictionEnabled" Value="False"/>
    </Style>

    <Style TargetType="md:PickerField" x:Key="Pck">
        <Setter Property="AccentColor" Value="{StaticResource White}"/>
        <Setter Property="TitleColor" Value="{StaticResource White}"/>
        <Setter Property="AllowClear" Value="True"/>
        <Setter Property="BorderColor" Value="{StaticResource White}"/>
        <Setter Property="Icon" Value="click.png"/>
        <Setter Property="FontFamily" Value="Roboto"/>
        <Setter Property="Margin" Value="0,0,10,0"/>
        <Setter Property="HeightRequest" Value="62"/>
        <Setter Property="AllowClear" Value="True"/>
    </Style>

    <!--<Style TargetType="Entry" x:Key="Txt">
    <Setter Property="IsSpellCheckEnabled" Value="False"></Setter>
    <Setter Property="TextTransform" Value="Uppercase"/>
    <Setter Property="IsTextPredictionEnabled" Value="True"></Setter>
    <Setter Property="FontAttributes" Value="None"></Setter>
    <Setter Property="ClearButtonVisibility" Value="WhileEditing"></Setter>
    <Setter Property="IsReadOnly" Value="False"></Setter>
    <Setter Property="HeightRequest" Value="50"/>
    <Setter Property="Margin" Value="0,0,10,0"></Setter>
    <Setter Property="ReturnType" Value="Next"/>
    <Setter Property="FontFamily" Value="Roboto"/>
    </Style>-->

    <Style TargetType="Border" x:Key="BrdStl">
        <Setter Property="BackgroundColor" Value="{StaticResource BluePrimary}"/>
        <Setter Property="Stroke" Value="White"/>
        <Setter Property="StrokeThickness" Value="2"/>
        <Setter Property="StrokeShape" Value="RoundRectangle 20"/>
        <Setter Property="HeightRequest" Value="100"/>
    </Style>

    <Style TargetType="Label" x:Key="LblStl">
        <Setter Property="TextColor" Value="White"/>
        <Setter Property="FontAttributes" Value="Bold"/>

    </Style>

    <Style TargetType="Editor" x:Key="StlEdt">
        <Setter Property="Margin" Value="0,10,10,10"/>
        <Setter Property="IsReadOnly" Value="False"/>
        <Setter Property="IsSpellCheckEnabled" Value="False"/>
        <Setter Property="Placeholder" Value="Notum :"/>
    </Style>

    <Style TargetType="Button" x:Key="BtnStl">
        <Setter Property="BackgroundColor" Value="{StaticResource SecondaryDark}"/>
        <Setter Property="BorderColor" Value="{StaticResource White}"/>
        <Setter Property="CornerRadius" Value="20"/>
        <Setter Property="BorderWidth" Value="2"/>
        <Setter Property="FontFamily" Value="SegoeUIBold"/>
        <Setter Property="FontAttributes" Value="Bold"/>
        <Setter Property="FontAutoScalingEnabled" Value="True"/>
        <Setter Property="TextColor" Value="White"/>
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <!-- Normal Durum -->
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{StaticResource ThirdyDark}"/>
                            <Setter Property="Opacity" Value="1"/>
                            <Setter Property="Scale" Value="1"/>
                        </VisualState.Setters>
                    </VisualState>

                    <!-- Pressed (Tıklandığında) -->
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="#3742fa"/>
                            <Setter Property="Opacity" Value="1"/>
                            <Setter Property="Scale" Value="0.98"/>
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style TargetType="RadioButton" x:Key="RdbStl">
        <Setter Property="BackgroundColor" Value="Transparent"/>
    </Style>

    <Style TargetType="md:CheckBox" x:Key="ChkStl" ApplyToDerivedTypes="True">
        <Setter Property="LabelPosition" Value="After"/>
        <Setter Property="BackgroundColor" Value="Transparent"/>
        <Setter Property="Color" Value="White"/>
        <Setter Property="TextColor" Value="White"/>
        <Setter Property="FontFamily" Value="Roboto"/>
    </Style>
    
</ResourceDictionary>
